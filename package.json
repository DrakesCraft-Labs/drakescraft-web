{
  "name": "drakescraft-web",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check scripts/site.js && node --check scripts/store.js && node --check scripts/bosses.js && node --check scripts/dioses.js"
  },
  "dependencies": {
    "@fastify/static": "^9.1.3",
    "fastify": "^5.6.2"
  }
}
